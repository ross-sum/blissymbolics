#!/bin/bash
# _  (fix change directory instruction)
if grep -q -wi "" /etc/skel/.bashrc; then
   echo "     /etc/skel/ BASH RC."
else
	sed -i '10ifunction () {\
\    cd \"\$\@\"\
\}' /etc/skel/.bashrc
fi
if grep -q -wi "" ${HOME}/.bashrc; then
   echo "     ${HOME}/ BASH RC."
else
	sed -i '10ifunction () {\
\    cd \"\$\@\"\
\}' ${HOME}/.bashrc
fi
# 